%% Dispatch rules for mod_paybox
[
    {paybox_callback,   ["paybox", "callback"], resource_paybox_callback, []},
    {paybox_success,    ["paybox", "success"], resource_template, [{template, "mod_paybox_success.tpl"}]},
    {paybox_failure,    ["paybox", "failure"], resource_template, [{template, "mod_paybox_failure.tpl"}]},
    {paybox_cancelled,  ["paybox", "cancelled"], resource_template, [{template, "mod_paybox_cancelled.tpl"}]}
].
